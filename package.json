{
  "name": "academic-portal-root",
  "version": "2.0.0",
  "description": "VSB Engineering College Academic Performance & Parent Notification Platform",
  "scripts": {
    "build": "npm --prefix client install --include=dev && npm --prefix client run build && npm --prefix server install --include=dev && npm --prefix server run prisma:generate && npm --prefix server run build",
    "start": "npm --prefix server run prisma:deploy && npm --prefix server run start:prod"
  },
  "keywords": [
    "academic-portal",
    "college",
    "education"
  ],
  "author": "VSB Engineering College",
  "license": "ISC"
}
